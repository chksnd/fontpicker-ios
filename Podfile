# Uncomment the next line to define a global platform for your project
platform :ios, '13.0'

target 'FontPicker' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  target 'FontPickerTests' do
    # Pods for testing
  end
end

target 'FontPickerDemo' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # pod 'FontPicker', :path => '.'
end
